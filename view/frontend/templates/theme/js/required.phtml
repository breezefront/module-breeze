<?php

/**
 * @var \Magento\Framework\View\Helper\SecureHtmlRenderer $secureRenderer
 */
$scriptString = <<<script
required.map((pair) => require(pair[0], pair[1])); required = [];
script;

echo /* @noEscape */ $secureRenderer->renderTag('script', ['data-breeze' => true, 'type' => 'module'], $scriptString, false);
